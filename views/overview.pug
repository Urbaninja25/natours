extends base

block content
  main.main
    .card-container
     
      
      each tour in tours
       
        .card
          .card__header
            .card__picture
              .card__picture-overlay &nbsp;
              
              img.card__picture-img(src=`/img/tours/${tour.imageCover}`, alt=`${tour.name}`)
            h3.heading-tertirary
              //- actual dynamic data 
              span= tour.name

          .card__details
            //- actual dynamic data.as u know alt expects some string.thats why we use template strings
            h4.card__sub-heading= `${tour.difficulty} ${tour.duration}-day tour`
            //- actual dynamic data
            p.card__text= tour.summary
            .card__data
              svg.card__icon
                use(xlink:href='/img/icons.svg#icon-map-pin')
              //- actual dynamic data  
              span= tour.startLocation.description
            .card__data
              svg.card__icon
                use(xlink:href='/img/icons.svg#icon-calendar')
              //- actual dynamic data.keep in mind that startdates is an arr of dates.and what we want in these overview is a start date of next tour+ ჩვენ გვინდა რო ეს დეითი უბრალოდ გამოიყურებოდეს როგორც მაგალითად june 2021 ან რაღაც ესეთი .ამიტომ with help of toLocaleString we convert it into nice readable string,and in here we pass an option of the language and object with some date options
              span= tour.startDates[0].toLocaleString('en-us', {month: 'long', year: 'numeric'})
            .card__data
              svg.card__icon
                use(xlink:href='/img/icons.svg#icon-flag')
              //- actual dynamic data.as u know alt expects some string.thats why we use template strings  
              span= `${tour.locations.length} stops`
            .card__data
              svg.card__icon
                use(xlink:href='/img/icons.svg#icon-user')
              //- actual dynamic data.as u know alt expects some string.thats why we use template strings  
              span= `${tour.maxGroupSize} people`

          .card__footer
            p
              //- actual dynamic data.as u know alt expects some string.thats why we use template strings 
              span.card__footer-value= `$${tour.price}`
              //- whenever we need a real space  between two inline elements(span for eg) we create these space using these piped line here above + აუცილებელია | ამას რო დაწერ კურსორი მიიტანო მასთან,წინ დაუსვა და space დააჭირორო იმუშაოს
              |  
              span.card__footer-text per person
            p.card__ratings
              //- actual dynamic data.
              span.card__footer-value= tour.ratingsAverage
              //- whenever we need a real space  between two inline elements(span for eg) we create these space using these piped line here above + აუცილებელია | ამას რო დაწერ კურსორი მიიტანო მასთან,წინ დაუსვა და space დააჭირორო იმუშაოს
              |  
              //- actual dynamic data.as u know alt expects some string.thats why we use template strings + აქ url ებს წინ / აქ როგორც ნახე.ასეთ url ს ქვია relative url and what these will do is that it will than add these pease of url after the host name
              span.card__footer-text= `rating (${tour.ratingsQuantity})`
            a.btn.btn--green.btn--small(href=`/tour/${tour.slug}`) Details